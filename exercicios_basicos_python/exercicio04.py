# Solicite que o usuário digite seu nome.
# Solicite que o usuários digite seu ano de nascimento (formato AAAA).
# Exiba na tela:
# nome_usuario nasceu em ano_nascimento!
# (nome_usuario e ano_nascimento são as variáveis)

nome_usuario=str(input("Digite o seu nome: "))
ano_nascimento=input("Digite sua data de nascimento: ")

print(f"{nome_usuario} nasceu em {ano_nascimento}")